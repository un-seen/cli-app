{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "go run ./tools/generate -config=configs/admin.yaml -output-dir=generated",
        "..."
      ]
    }
  }
}
